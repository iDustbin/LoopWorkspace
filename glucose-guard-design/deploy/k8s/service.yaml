apiVersion: v1
kind: Service
metadata:
  name: glucose-guard-web
  namespace: glucose-guard
  labels:
    app.kubernetes.io/name: glucose-guard-web
spec:
  selector:
    app.kubernetes.io/name: glucose-guard-web
  ports:
    - name: http
      port: 80
      targetPort: http
